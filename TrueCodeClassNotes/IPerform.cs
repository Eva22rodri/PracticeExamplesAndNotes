
namespace TrueCodeClassNotes
{
    interface IPerform //Interface is difference form a class.. Always name starting with the letter 'I'.
    {
        // no implementation defined,
        // only the requirement that there be a void method call
        //"perform" that takes no parameters
        void Perform(); //There should be method called perform that can(but doesnt need to) take some parameter       
    }
}
